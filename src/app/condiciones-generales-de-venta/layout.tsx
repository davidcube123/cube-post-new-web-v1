import type { Metadata } from "next";
export const metadata: Metadata = { title: "Condiciones generales de venta", alternates: { canonical: "/condiciones-generales-de-venta/" } };
export default function LegalLayout({children}: {children: React.ReactNode}) { return children; }
